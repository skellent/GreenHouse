{
  "name": "greenhouse",
  "version": "3.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun tools/dev-server.js",
    "build": "bun tools/check-static.js",
    "preview": "bun tools/dev-server.js",
    "cap:add:android": "bunx cap add android",
    "cap:sync": "bun run build && bunx cap sync android",
    "cap:open": "bunx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/core": "latest",
    "@capacitor/filesystem": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest"
  }
}
